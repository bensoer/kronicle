# clusterrolebinding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kronicle.fullname" . }}-binding
subjects:
- kind: ServiceAccount
  name: {{ include "kronicle.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }} # Namespace where your app runs
roleRef:
  kind: ClusterRole
  name: {{ include "kronicle.fullname" . }}-cluster-role
  apiGroup: rbac.authorization.k8s.io